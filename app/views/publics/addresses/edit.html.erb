<h5>配送先編集</h5>
<%= form_with model: @address, local:true do |f| %>
郵便番号(ハイフンなし):<%= f.text_field :postal_code %><br>
住所:<%= f.text_field :address %><br>
宛名:<%= f.text_field :name %>　　<%= f.submit "編集内容を保存" %>
<% end %>